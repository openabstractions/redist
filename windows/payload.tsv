> Every file the package installs, and which feature puts it there.
>
> feature	service | tools | developer
> path		where it lands under the install folder, and the staging path the
>		workflow builds: payload/<path>, which is what File@Source says
> kind		gobuild  go build the package at <from> in <source>
>		copy     take the file at <from> out of <source>
>		authored written here, in installer/, and shipped as it stands
> source	an id from sources.tsv
> from		the path inside that source
> sign		Authenticode target. Only a PE file can be one; every other row is
>		"no" and the two MSIs are signing targets in their own right
> subsystem	console | windows on a gobuild row, "-" on every other, because a
>		file that is not linked has no subsystem. "windows" appends
>		-H=windowsgui to the link, which is the only way to build an image
>		the loader gives no console. It is a property of the image and not
>		of the package or of its source: one package can be linked both
>		ways, and a row is the only place that choice is written down
>
> A row whose source has no published commit yet is still built by a --src build
> and still left out of a --bin one. sources.tsv says which sources those are.
> A row the --bin route cannot fill must be gated in abstraction.wxs, because a
> gate is what turns a missing file into a smaller package instead of a red
> build. openabstractions.exe and openabstractionsw.exe are the console and
> windowless links of the one serve source, and both rows are required: the
> windowless image is what the service template, the Startup shortcut and the
> installer's activation run. jobd,
> jobdw, dl, jobctl and the Python file-store packages leave the payload in
> 0.2.0 (docs/REMOVED.md).
>
> over_curl.hpp has a declared abstraction-download-over-curl repository rule,
> but no public refs were present at the 2026-09-12 check. Until a public commit
> is verified, its optional source stays UNRESOLVED.
>
> The Panel ships no side-by-side manifest. Its two elements were a dpiAware
> setting that monitor/win/win_windows.go already applies by calling
> SetProcessDpiAwarenessContext, and a Common-Controls 6.0.0.0 dependency that
> names processorArchitecture="amd64" while this package is also built for arm64.
> It was also a copy row against the same source as the program, so a --bin
> release build could never obtain it, and its absence turned the whole Panel
> feature off and took the program with it. monitor/README.md documents the
> manual copy for anyone who wants the button styling in a local build.

feature		path						kind		source		from						sign	subsystem
tools		tools/Abstraction Panel.exe			gobuild		panel		monitor						yes	windows
tools		examples/README.txt					authored	installer	examples/README.txt				no	-
tools		examples/what-this-machine-does/run.cmd		authored	installer	examples/what-this-machine-does/run.cmd		no	-
tools		examples/job-lifecycle/run.cmd			authored	installer	examples/job-lifecycle/run.cmd			no	-
tools		examples/download-a-file/run.cmd		authored	installer	examples/download-a-file/run.cmd		no	-
developer	dev/USING.txt					authored	installer	dev/USING.txt					no	-
developer	dev/include/abstraction/download/over_curl.hpp	copy		UNRESOLVED	include/abstraction/download/over_curl.hpp	no	-
service	tools/openabstractions.exe	gobuild	charter	serve	yes	console
service	tools/openabstractionsw.exe	gobuild	charter	serve	yes	windows
